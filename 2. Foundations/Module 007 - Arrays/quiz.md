# Quiz – Arrays
```javascript
1. What is the index of the first item in an array?
// the first item in an array is always 0.
2. What does `.push()` do?
// The .push() method in JavaScript is used to add one or more elements to the end of an array.
3. What does `.pop()` return?
// removes the last element from an array and returns it. 
4. What does `.length` return?
// .lenth determines the number of elements in an array.
5. How do you access the third item in this array?

let snacks = ["apple", "banana", "carrot"];

//let thirdSnack = snacks[2];
//console.log(thirdSnack);
// this should output carrot.
```